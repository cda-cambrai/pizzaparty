/**
 * Cette classe me permet de structurer ce que
 * contient une pizza. L'export permettra d'importer
 * la classe partout dans mon application.
 */
export class Pizza {
    id: number;
    name: string;
    price: number;
    image: string;
}
